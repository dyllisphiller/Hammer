<Page
    x:Class="Hammer.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:fi="using:Fluent.Icons"
    xmlns:local="using:Hammer.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <StackPanel Margin="0,12,0,0">
            <TextBlock x:Name="Settings_Header" Text="Settings" Style="{StaticResource HeaderTextBlockStyle}" Margin="0,0,0,12"/>
            <TextBlock x:Name="Privacy_Header" Text="Privacy" TextWrapping="Wrap" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,6"/>

            <ToggleSwitch Header="Keep search history" OnContent="Keep" OffContent="Forget" x:Name="KeepSearchHistory" IsOn="{x:Bind Mode=TwoWay, Path=Settings.KeepSearchHistory}"/>

            <Button x:Name="ClearSearchHistoryButton" Style="{StaticResource ButtonRevealStyle}">
                <StackPanel Orientation="Horizontal">
                    <fi:FluentIconElement Symbol="Delete20" Margin="0,0,8,0"/>
                    <TextBlock Text="Clear search history"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</Page>
